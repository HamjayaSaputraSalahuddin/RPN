// Looping with Conditional Statement

let i = 0;

while (i < 30) {
    if (i % 2 === 0) {
        console.log(`${i} adalah genap`);
    } else {
        console.log(`${i} adalah ganji`);
    }
    i++
}